{
  "name": "convenience-store-frontier",
  "version": "0.1.0",
  "private": true,
  "description": "Convenience-store Frontier - numeric simulation core (Milestone 1)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit false --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
