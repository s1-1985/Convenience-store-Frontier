# AndroidデモAPKのインストールエラー

旧デモ版が固定署名導入前のビルドである場合、新しい固定署名版を最初に入れる時だけ旧版のアンインストールが必要になる。

手順：

1. 端末の設定から既存の「Convenience-store Frontier」をアンインストールする
2. 固定署名版APKをインストールする
3. 以後は新しい固定署名版APKを上書きインストールする

固定署名版導入後は、同じapplicationIdと同じ署名証明書を使い、versionCodeをビルドごとに増加させるため、通常の上書き更新が可能になる。
