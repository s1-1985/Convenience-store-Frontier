# Demo signing key

`demo-debug.keystore.b64` is a public, development-only Android debug signing key for installable demo APKs.

It must never be used for Google Play or production releases. Its sole purpose is to keep local demo builds update-compatible so testers do not lose app data on every APK update.
